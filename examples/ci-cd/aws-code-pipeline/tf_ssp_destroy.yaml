version: 0.2

#env:
#  variables:
#    key: "value"
#    key: "value"

phases:
  install:
    commands:
      - "curl -s -qL -o terraform.zip https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip"
      - "unzip -o terraform.zip"
      - mv terraform /bin
      - rm terraform.zip

  build:
    commands:
      - cd templates/source
      - terraform destroy -input=false tfapply
  post_build:
    commands:
      - echo "Nothing to do in the  post_build for now"
